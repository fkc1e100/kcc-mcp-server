{
  "name": "kcc-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for contributing to k8s-config-connector with enforced attribution rules",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "npm run build && node test/test-urlmap-example.js",
    "postinstall": "npm run build"
  },
  "keywords": [
    "mcp",
    "k8s-config-connector",
    "kubernetes",
    "gcp"
  ],
  "author": "Frank Currie",
  "license": "Apache-2.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "yaml": "^2.3.4",
    "typescript": "^5.3.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
